// Return an array without any values, but of a given length.

// The length value will always be greater than 0.
// This exercise is to showcase a behaviour of a certain javascript feature, to use it carefully.
// Note the output generated by the function.
// For example:

// Input: 10

// Output: [null, null, null, null, null, null, null, null, null, null]

 const createArray = (length) => new Array(length);